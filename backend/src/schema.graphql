type Mutation {
  createUser(name: String!): User!
}

type Query {
  user: User
  googlePlaceWithId(id: String!): GooglePlace
}

type User {
  id: ID!
  name: String!
}

type GooglePlace {
  payload: String!
}
